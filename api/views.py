from django.shortcuts import render
def index(request):
    # ランダムに5セットのクイズを取得
    # quizzes = Quiz.objects.order_by('?').[:5]
    # context = {
    #     'quizzes': quizzes,
    # }
    # return
    pass